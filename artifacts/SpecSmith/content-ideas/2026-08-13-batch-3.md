# Content Batch — Week of Aug 13, 2026

Format: 5 hooks + weird combos to screen-record live on specsmithpc.com.
Every number here is real, pulled straight from the site's actual dataset —
nothing staged. This batch leans on two features that shipped since batch 2:
the Home Office/Productivity guide and the GPU Trade-Up Calculator.

## Hooks (pick whichever fits the visual you record)

1. "This $688 PC build still uses a $139 graphics card. On purpose."
2. "A $699 CPU. A $476 CPU. Nearly the same FPS."
3. "I typed in my old GPU and it told me exactly what to buy next."
4. "This 2022 flagship costs more than a 2025 card almost as fast — again."
5. "Rolled the dice on a random PC build."

## Weird/funny combos (real parts, real prices)

**1. The Home Office flex (pairs with hook #1)**
`/best-pc-for/home-office` — all three tiers (Everyday Office $338, Power
Multitasker $538, Hybrid Work + Light Creative $688) use the exact same
$139 RX 6400 GPU. The site deliberately spends nothing extra on graphics
for office work and puts every dollar into CPU cores instead — the top
tier's CPU alone (i9-14900KS, $549, 24 cores) costs nearly 4x the GPU.
Screen-record scrolling all three tiers side by side — the same GPU line
repeating three times is the whole joke.

**2. The CPU flagship trap (pairs with hook #2)**
Ryzen 9 7950X3D — $699, 16 cores, benchmark 295 — vs Ryzen 7 9800X3D — $476,
8 cores, benchmark 290. Half the cores, $223 cheaper, within 2% of the same
gaming benchmark score. Pull both up on the CPU Tier List (both land in
S-tier) and let the numbers sit on screen — good beat: freeze on the price
difference right after showing how close the scores are.

**3. The Trade-Up Calculator reveal (pairs with hook #3)**
Type in RTX 3060 ($279 new, common older/budget card) on
`/upgrade-calculator`. Estimated resale: $180. Net cost to jump to an RTX
5060 Ti: $384. Screen-record the actual calculator working — select the
card, watch the upgrade options populate live. No commentary needed, the
"here's exactly what it'd cost you" moment is the hook.

**4. The GPU generational rematch (pairs with hook #4)**
RTX 4080 — $1,022, released 2022, benchmark 235 — vs RTX 5070 Ti — $919,
released 2025, benchmark 225. The three-year-old card costs $103 more than
a current-gen card within 5% of its performance. Same beat as batch 2's
3090 Ti/5070 pairing but a fresh pair of cards, so it doesn't feel repeated
to anyone who saw both — play it directly on the real matchup page,
`/vs/rtx-5070-ti-vs-rtx-4080`.

**5. The Build Crate pull (pairs with hook #5)**
Open `/crate`, let the real sequential reveal play out unedited — don't
pre-decide or fake a good pull, the site's own gacha system does the work.
This is the third batch to use this hook type; it's meant to be recurring
since every pull is genuinely different. Caption template: "rolled the
dice on a random PC build" + whatever the real result says.

## How to shoot it
15-20 seconds, screen recording only, no talking needed:
1. Land on the relevant page (`/best-pc-for/home-office`, `/cpu-tier-list`,
   `/upgrade-calculator`, `/compare` or the matchup page, or `/crate`)
2. For the calculator and crate hooks (#3, #5), don't cut until the real
   result lands on screen — the payoff is the actual number/reveal
3. On-screen text = the hook, nothing more
4. For the Home Office and CPU trap hooks (#1, #2), a slow scroll/zoom on
   the price and spec numbers side by side is enough — let the numbers do
   the talking
