# Content Batch — Week of Aug 16, 2026

Format: 5 hooks + weird combos to screen-record live on specsmithpc.com.
Every number here is real, pulled straight from the site's actual dataset —
nothing staged. This batch leads with the PC Build Quiz, which shipped
since batch 3.

## Hooks (pick whichever fits the visual you record)

1. "I answered 2 questions and it picked my entire build."
2. "This $689 card beats a $1,299 card. Same brand doesn't mean same deal."
3. "16 cores vs 8 cores. 3% apart in gaming."
4. "Every card, ranked, no opinions involved."
5. "Rolled the dice on a random PC build."

## Weird/funny combos (real parts, real prices)

**1. The Quiz reveal (pairs with hook #1)**
Open `/quiz`, pick "Gaming," pick a budget tier, screen-record the actual
2-click flow landing on a real GPU + CPU pick with real prices — then hit
"Load in Builder" and show it drop straight into the full builder with
those parts pre-selected. No commentary needed; the whole hook is "this
took 2 clicks." First batch to feature this since it shipped.

**2. The AMD upset (pairs with hook #2)**
RX 9070 XT — $689, benchmark 222, released 2025 — vs RTX 4070 Ti Super —
$1,299, benchmark 215, released 2024. The cheaper card scores *higher*.
Pull up the real matchup page, `/vs/rtx-4070-ti-super-vs-rx-9070-xt`, and
let the side-by-side numbers sit on screen — freeze right as the price
gap and the benchmark line both come into frame.

**3. The core-count non-story (pairs with hook #3)**
Ryzen 9 9950X3D — $669, 16 cores, benchmark 310 — vs Ryzen 7 9850X3D —
$499, 8 cores, benchmark 300. Double the cores for $170 more buys you
about 3% more gaming performance. Pull both up on the CPU Tier List
(both land in S-tier) and hold on the cores column — that's the whole
joke, half the cores barely move the number.

**4. The Tier List scroll (pairs with hook #4)**
`/gpu-tier-list` — slow scroll from S-tier straight down to D-tier, no
cuts. Every card we track, ranked purely by benchmark score, with the 💰
best-value pick flagged in each tier. Works as a satisfying "here's
everything, ranked" clip on its own with zero editing.

**5. The Build Crate pull (pairs with hook #5)**
Open `/crate`, let the real sequential reveal play out unedited — don't
pre-decide or fake a good pull, the site's own gacha system does the work.
Fourth batch to use this hook type; it's meant to be recurring since every
pull is genuinely different. Caption template: "rolled the dice on a
random PC build" + whatever the real result says.

## How to shoot it
15-20 seconds, screen recording only, no talking needed:
1. Land on the relevant page (`/quiz`, the matchup page, `/cpu-tier-list`,
   `/gpu-tier-list`, or `/crate`)
2. For the quiz and crate hooks (#1, #5), don't cut until the real result
   lands on screen — the payoff is the actual pick/reveal
3. On-screen text = the hook, nothing more
4. For the AMD upset and core-count hooks (#2, #3), a slow zoom on the
   price and benchmark numbers side by side is enough — let the numbers
   do the talking
