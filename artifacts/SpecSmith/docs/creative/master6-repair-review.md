# MASTER #6 independent repair

Starting checkpoint: `e81d726301614cfa69f7d3554b3aefb068c5d76e`.

## Repaired integrity and integration gaps

- Measured creative memory requires a resolved MASTER #5 experiment/result/candidate, matching identity and permission, all mandatory guardrails, and the actual tested decision/value. Caller-written strength and observations cannot substitute for those records.
- Retrieval re-resolves evidence and requires exact audience, platform, topic, mission and objective scope. Withdrawn, superseded, contradictory and duplicate records cannot inflate guidance. A losing variant is context, not a recommendation to reuse it.
- Immutable local JSON records distinguish production from synthetic engineering memory. Exact replay is idempotent; contradictory replay requires a new revision ID.
- Estimates and measurement-like illustrations require their specific canonical disclosures during every relevant beat. The same disclosure text survives storyboard conversion.
- Reference price scripts describe CPU-plus-GPU editorial totals, not complete builds or live purchase quotes. Unsupported platform longevity claims and the whole-purchase game-category shortcut are removed. The model band is a convention, not calibrated uncertainty.
- A new Compare mission with empty memory produces three structurally distinct editorial scaffolds, using an evidence-backed MASTER #2 claim and a catalog-validated render state. Relabeling an identical treatment cannot satisfy divergence; genuinely different treatments may reach the same honest takeaway.
- Eligible proposals reach the existing production-plan and quality-review contracts. Every visual task is bound to the exact validated Compare state; no reference-CPU substitution or generated-video fallback is introduced.
- The offline pipeline separates this new proposal/contract demonstration from its existing hand-authored rendered-video smoke test. It no longer requires one package to remain permanently blocked.
- Unknown model ranges cannot be reported as entirely inseparable evidence.

## Verification and remaining limits

Local v2 suite: 830 tests across 38 files passed. Typecheck passed. Disabling the tested-decision guard and the per-beat disclosure guard individually caused the relevant regression assertion to fail; both were restored and the suite rerun green. Exact-head full CI remains pending at submission.

This is **not** evidence that MASTER #6 is a maximum-creativity engine. The new proposal pass is deterministic editorial scaffolding, not a generative-model provider or a semantic originality evaluator. It currently supports Compare missions and selects the first grounded claim. Structure checks do not establish entertainment value, originality or retention.

Generated proposal video rendering, readability review, independent human creative review and media/audio approval remain outstanding. The annotated vertical spec-card capability remains absent, explicitly blocked rather than silently approximated. No provider calls, publishing, scheduling, main-branch merge or MASTER #7 work are authorized by this repair.
