kind = "web"
previewPath = "/"
title = "Specsmith"
version = "1.0.0"
id = "artifacts/SpecSmith"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 25360

[services.development]
run = "pnpm --filter @workspace/specsmith run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/specsmith", "run", "build" ]
publicDir = "artifacts/SpecSmith/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "25360"
BASE_PATH = "/"
