// Bump this whenever gpus.json / cpus.json / components.json prices get a
// refresh pass. Shown next to price displays so users know how current the
// estimates are.
export const PRICES_UPDATED = 'July 13, 2026';
